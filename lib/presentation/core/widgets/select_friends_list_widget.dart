import 'package:flutter/material.dart';

class SelectFriendsListWidget extends StatelessWidget {
  const SelectFriendsListWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
