import 'package:flutter/material.dart';

class FriendCardClickableWidget extends StatelessWidget {
  const FriendCardClickableWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
